.DS_Store
/Candela.xcodeproj
/DerivedData
.build/
*.xcuserstate
/Candela/Info.plist
/build/

# per-worktree scratch DerivedData used by the deploy loop
dd/

# Local secrets (Cloudflare token etc.); never commit
.env

