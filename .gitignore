.DS_Store
/Candela.xcodeproj
/Candela/Info.plist
/DerivedData
/build/
.build/
*.xcuserstate
xcuserdata/

# Scratch DerivedData for a build that must not touch the warm cache:
#   make release DD=dd
dd/

# Local secrets (Cloudflare token etc.); never commit
.env
